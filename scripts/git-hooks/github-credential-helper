#!/bin/sh
# Git credential helper: supplies GITHUB_PERSONAL_ACCESS_TOKEN for github.com.
# Git calls this with "get", "store", or "erase" as the first argument.
if [ "$1" = "get" ]; then
  printf "username=x-access-token\n"
  printf "password=%s\n" "${GITHUB_PERSONAL_ACCESS_TOKEN}"
fi
